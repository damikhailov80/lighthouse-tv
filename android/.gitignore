.gradle/
build/
local.properties
.idea/
*.iml
app/src/main/assets/www/
# The illustrations, copied out of src/assets by sync-web.sh for the
# home-screen channel. Generated, so the originals stay the only copy in git.
app/src/main/res/drawable-nodpi/
