<?xml version="1.0" encoding="utf-8"?>
<!--
  targetSdk 34 blocks plain HTTP by default, which is right: the deployed API is
  reached over HTTPS and nothing here relaxes that.

  What is opened up is the two addresses a build points at while it is being
  worked on — the emulator's route to the host machine, and localhost. Both are
  named explicitly rather than allowing cleartext in general, so pointing a
  television at an http:// server by accident still fails.

  A LAN address belongs here too if the API is ever served from a box in the
  house without a certificate. Add it by name; do not widen base-config.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
